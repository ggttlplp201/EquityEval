# Frontend

Read root AGENTS.md and docs/spec.txt. The browser renders values supplied by
the API. Arithmetic is limited to display formatting; valuation and ratios are
never computed here. Use the reviewed generated API types after S6.
Every displayed financial number needs provenance and staleness information.
Keep missing values visible as gaps with quality flags. Keep historical modes
separate and assumption changes versioned. X1 has an authorized development
UI over explicitly fictional, offline Python results; its local presentation
types are not the S6 public API. Production `/sectors` stays unavailable until
its recorded source and contract prerequisites are met. Keep fixtures visibly
fictional and never mix them with normal source storage. Read README.md.
